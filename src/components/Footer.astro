---
export interface Props {
  t: {
    // We can add footer translations later if needed
  };
}

const { t } = Astro.props;
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-left">
        <h3 class="footer-title">IOAI Spain</h3>
        <div class="footer-links">
          <span class="footer-year">© {currentYear} Harbour.Space</span>
          <a target="_blank" href="https://harbour.space/privacy-policy" class="footer-link">Privacy Policy</a>
          <a href="mailto:ioai.spain@harbour.space" class="footer-link">ioai.spain@harbour.space</a>
        </div>
      </div>

      <div class="footer-right">
        <span class="powered-by">Powered by:</span>
        <span class="powered-by-text">Harbour.Space Institute of Technology</span>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-inline: var(--section-inline);
    background: var(--background-primary);
  }

  .container {
    border-top: 1px solid var(--border-primary);
    max-width: var(--max-width);
    padding-block: var(--space-l);
    margin: 0 auto;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--space-l);
  }

  .footer-left {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .footer-title {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-medium);
    color: var(--text-primary);
    margin: 0;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: var(--space-m);
    flex-wrap: wrap;
  }

  .footer-year {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
  }

  .footer-link {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-link:hover {
    color: var(--color-primary);
  }

  .footer-right {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    text-align: right;
  }

  .powered-by {
    font-size: var(--font-size-sm);
    color: var(--text-secondary);
  }

  .powered-by-text {
    font-size: var(--font-size-sm);
    color: var(--text-primary);
    font-weight: var(--font-weight-medium);
  }

  @media (max-width: 809px) { /* mobile only */
    .footer-content {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-m);
    }

    .footer-right {
      align-self: flex-start;
      text-align: left;
    }

    .footer-links {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-xs);
    }
  }
</style>

